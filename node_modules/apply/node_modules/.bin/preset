#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../@preset/cli/bin/preset.mjs" "$@"
  ret=$?
else 
  node  "$basedir/../../../@preset/cli/bin/preset.mjs" "$@"
  ret=$?
fi
exit $ret
