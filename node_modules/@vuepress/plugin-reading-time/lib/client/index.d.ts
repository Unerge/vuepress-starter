import { ComputedRef } from 'vue';
import { ExactLocaleConfig } from '@vuepress/helper/shared';

interface ReadingTime {
    /**
     * expect reading time
     *
     * 期望的阅读时间
     */
    minutes: number;
    /**
     * words of current page
     *
     * 当前页的字数
     */
    words: number;
}
interface ReadingTimePluginPageData {
    readingTime: ReadingTime;
}

/**
 * Multi language config for `@vuepress/plugin-reading-time` plugin
 *
 * `@vuepress/plugin-reading-time` 插件的多语言配置
 */
interface ReadingTimePluginLocaleData {
    /**
     * Word template, `$word` will be automatically replaced by actual words
     *
     * 字数模板，模板中 `$word` 会被自动替换为字数
     */
    word: string;
    /**
     * Text for less than one minute
     *
     * 小于一分钟文字
     */
    less1Minute: string;
    /**
     * Time template
     *
     * 时间模板
     */
    time: string;
}
type ReadingTimePluginLocaleConfig = ExactLocaleConfig<ReadingTimePluginLocaleData>;

declare const useReadingTimeData: () => ComputedRef<ReadingTime | null>;

declare const useReadingTimeLocaleConfig: () => ComputedRef<ReadingTimePluginLocaleData | null>;
interface ReadingTimeLocale {
    /**
     * Reading time text
     *
     * 阅读时间文字
     */
    time: string;
    /**
     * Words count text
     *
     * 字数统计文字
     */
    words: string;
}
declare const useReadingTimeLocale: () => ComputedRef<ReadingTimeLocale>;

declare const getReadingTimeLocale: (readingTime: ReadingTime, locale: ReadingTimePluginLocaleData) => {
    time: string;
    words: string;
};

export { type ReadingTime, type ReadingTimeLocale, type ReadingTimePluginLocaleConfig, type ReadingTimePluginLocaleData, type ReadingTimePluginPageData, getReadingTimeLocale, useReadingTimeData, useReadingTimeLocale, useReadingTimeLocaleConfig };
